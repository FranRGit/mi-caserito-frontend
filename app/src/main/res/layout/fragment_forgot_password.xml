<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingHorizontal="32dp"
        android:paddingTop="32dp"
        android:paddingBottom="32dp"
        android:gravity="center_horizontal">

        <!-- Back -->
        <ImageView
            android:id="@+id/btnBackForgot"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="@drawable/bg_circle_green"
            android:src="@drawable/ic_arrow_back"
            app:tint="@color/white"
            android:padding="8dp"
            android:layout_gravity="start"
            android:layout_marginBottom="24dp"/>

        <!-- Lock Icon -->
        <ImageView
            android:id="@+id/ivLockIcon"
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:src="@drawable/ic_lock"
            android:tint="@color/primary_color"
            android:layout_marginBottom="24dp"/>

        <!-- Title -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="¿Olvidaste tu Contraseña?"
            android:textColor="@color/black"
            android:textStyle="bold"
            android:textSize="22sp"
            android:layout_marginBottom="10dp"/>

        <!-- Subtitle paragraph -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="¡No te preocupes! Por favor ingresa el correo electrónico asociado con tu cuenta"
            android:textColor="#7A7A7A"
            android:textSize="14sp"
            android:gravity="center"
            android:layout_marginBottom="28dp"/>

        <!-- Email input -->
        <EditText
            android:id="@+id/etForgotEmail"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:hint="Ingresa tu correo electrónico"
            android:background="@drawable/bg_input_field"
            android:backgroundTint="#F5F5F5"
            android:paddingHorizontal="16dp"
            android:textSize="14sp"
            android:textColor="@color/black"
            android:layout_marginBottom="28dp"/>

        <!-- OTP Button -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnGetOtp"
            android:layout_width="280dp"
            android:layout_height="52dp"
            android:text="Obtener OTP"
            android:textColor="@color/white"
            android:textStyle="bold"
            android:textAllCaps="false"
            android:textSize="16sp"
            app:cornerRadius="10dp"
            app:backgroundTint="@color/primary_color"
            android:layout_marginBottom="16dp"/>

        <!-- Bottom text -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="OTP = Contraseña de un solo uso (código de verificación)"
            android:textColor="#9A9A9A"
            android:textSize="12sp"/>

    </LinearLayout>
</ScrollView>
